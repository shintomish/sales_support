# メール保持・削除フロー

> 対象読者：当社営業担当者
> 元データ：2026-04-06 打合せ資料(案件マーケット連携フローについての補足説明)
> 最終更新：2026-05-30

---

## 2026-05-25〜30 更新ノート

- **received_at の Date ヘッダー優先化（2026-05-28）**: 従来は IMAP `INTERNALDATE` を `received_at` に採用していたが、Kagoya 環境では `INTERNALDATE` と `Date` ヘッダーに最大 9 時間のズレが発生していた。Date ヘッダー優先（fallback で INTERNALDATE）に変更し、受信時刻の精度を改善。これにより Kagoya 受信側でも平均 2.6 時間程度の配送遅延が常態化していることが可視化された。
- **Kagoya IMAP UID ベース増分取得（2026-05-29）**: `KagoyaMailService::syncEmails` の取込窓を sequence ベースから **UID ベース**に切替。直近 24 時間で 220 UID gap、最大で 2,236 件 gap が発生していた構造欠陥を根治。15 分毎の自動同期で取りこぼしが発生しなくなった。
- **bounce retention 7 → 3 日（2026-05-29）**: `Cleanup` ジョブの bounce メール保持期間を短縮 + 1 回 500 件のバッチ化（pg_trgm GIN index 保守による statement_timeout 回避）。
- **全件既読の非同期化（2026-05-26）**: 同期 API は数千件 未読で 502（nginx タイムアウト）になっていたため、`mark-all-read` を Schedule 駆動の非同期ジョブ化（rescore-all と同パターン）。`mark-read-status` で進捗ポーリング。200 件ずつのバッチ + `SET LOCAL statement_timeout=5min` で安全に処理。
- **添付ファイル名衝突（2026-05-27 修正）**: IMAP 取込で複数添付が同名（例: `unknown.bin`）で来た場合、Storage `storage_path` が衝突して後勝ち上書きされていたバグを解消（`KagoyaMailService` の attachment 処理に index suffix を付与）。

---

## 0. 本書のカバー範囲

受信メールの自動処理スケジュールとデータ保持期間のルールを、営業担当者向けに平易に説明する。

技術的な詳細仕様は以下を参照:
- [410_feature_design.md](./410_feature_design.md) の「6. スケジューラー設定」「7. メール保持ポリシー」
- [540_project_mail_flow.md](./540_project_mail_flow.md) — 案件メール画面の運用
- [530_engineer_mail_flow.md](./530_engineer_mail_flow.md) — 技術者メール画面の運用

---

## 1. 自動処理のスケジュール

システムは以下のタイミングで自動的にメールを処理します。

| 処理内容 | 実行タイミング |
|----------|--------------|
| 新着メールの取り込み（Gmail / KAGOYA IMAP それぞれ） | 15分ごと |
| 案件メール／技術者メールへの自動仕分け | 15分ごと |
| 案件メール画面への取込・スコアリング | 15分ごと |
| 技術者メール画面への取込・スコアリング | 15分ごと（100件/回） |
| 処理済みメールをGmailのゴミ箱へ移動 | 毎日 午前2時 |
| 古いメールの本文削除・レコード削除 | 毎日 午前3時 |

> システムが自動で動くため、担当者が手動で操作する必要はありません。
> ※ 旧バージョンにあった「AIによる抽出（30分毎）」は廃止し、案件・技術者それぞれの専用スコアリングに移行しています。

---

## 2. メールの保持期間とデータ削除ルール

サーバーの容量を圧迫しないよう、以下のルールで自動削除しています。

| 対象 | 経過期間 | 行われる処理 |
|------|----------|-------------|
| 「案件メール」または「技術者メール」に仕分けされたメール | 30日後 | メールの本文を削除（件名・差出人は残る） |
| 「案件メール」または「技術者メール」に仕分けされたメール | 90日後 | メール自体を完全削除 |
| 仕分けされなかったメール（未分類） | 14日後 | メール自体を完全削除（取りこぼしとみなす） |

### 削除後も案件履歴は残ります

メールが削除されても、**「誰から・どんな案件が届いたか」のメタ情報は「案件メール」画面に永続保存**されます。
メール本文が消えた後も、案件の受信履歴は引き続き確認できます。

---

*打合せ記録：2026-04-06 / ドキュメント整形：2026-04-22 / 2026-04-29 自動処理スケジュール同期*
