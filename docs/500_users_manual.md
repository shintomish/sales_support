# 営業支援システム ユーザーマニュアル

> 作成日: 2026-05-03 / 対象: 社内リリース 2026-05-07 〜 / バージョン: 1.0

---

## 1. はじめに

### 1.1 本マニュアルの目的

営業支援システム（Sales Support System）の社内利用にあたり、画面操作の基本と各機能の使い方をまとめたガイドです。  
PPT 化して新規利用者向けの説明資料として活用できる構成にしています。

### 1.2 対象ユーザー

- 営業担当者（メール処理・配信・マッチング業務）
- 経理担当者（SES 契約・請求書発行）
- 管理者（テナント・ユーザー管理）

### 1.3 アクセス先

| 環境 | URL |
|---|---|
| 本番 | https://app.ai-mon.net |
| 開発（参考） | （社内告知済 URL） |

### 1.4 SES プラン限定機能の表記

本マニュアルでは、SES 契約管理機能を有効化したテナントのみ利用できる項目に **【SES限定】** を、管理者ロールのみ利用できる項目に **【管理者限定】** を付与しています。

![ログイン画面](images/login.png)

---

## 2. ログインとパスワード管理

### 2.1 ログイン

1. ブラウザで `https://app.ai-mon.net` を開く
2. 登録済みのメールアドレスとパスワードを入力
3. 「ログイン」をクリック

### 2.2 パスワードを忘れた場合

1. ログイン画面下部の「パスワードを忘れた方はこちら」をクリック
2. 登録メールアドレスを入力 → 送信
3. 受信メールのリンクからパスワードリセット画面へ
4. 新パスワードを入力して確定

> 認証メールは `outsource@aizen-sol.co.jp` から送信されます（AWS SES 経由・日本語テンプレート）。

### 2.3 ログアウト

サイドバー下部のユーザー情報直下にある **「↩ ログアウト」** ボタンをクリック。

![パスワードリセット画面](images/forgot-password.png)

---

## 3. ダッシュボード

ログイン直後に表示される画面。各機能の概況とアラートを一目で把握できます。

### 3.1 主な表示要素

- 未読メール件数（メールアイコンに赤バッジ）
- 期限切れタスク件数（赤いタスクカード）
- 直近の活動履歴
- 配信キャンペーンの進捗（送信中の場合）

### 3.2 サイドバーからの遷移

ダッシュボードは常にメインメニュー先頭に表示されます。  
緊急対応が必要な項目（期限切れタスクなど）は赤バッジで視覚化されています。

![ダッシュボード](images/dashboard.png)

---

## 4. CRM

### 4.1 顧客管理（`/customers`）

#### 機能
取引先企業の登録・編集・検索。  
案件メールの自動取込で `customer_name` と紐付くことがあります。

#### 主な操作
- **一覧**: 検索（会社名）/ 並び替え / フィルタ
- **新規登録**: 「+ 新規登録」ボタンから登録フォームへ
- **編集**: 一覧の行をクリック → 詳細画面 → 「編集」
- **削除**: 詳細画面の「削除」ボタン（関連データに注意）

#### 関連
- 商談 / 担当者 / 活動履歴 が顧客にひも付きます

### 4.2 担当者管理（`/contacts`）

#### 機能
取引先の担当者個人を管理。顧客に紐付け。

#### 主な操作
- 顧客選択 → 担当者を新規登録
- 名刺管理から担当者を自動生成（OCR で氏名・会社・メール抽出）
- メールアドレス重複チェックあり

### 4.3 商談管理（`/deals`）

#### 機能
案件単位の商談（ディール）を管理。ステータスで進行状況を追跡。

#### 主なステータス
- 新規 / 提案中 / 面談中 / 受注 / 失注

#### 主な操作
- 顧客 + 案件タイトル で新規作成
- 商談ステータスを更新（一覧またはドラッグ＆ドロップ）
- 関連メール / 活動履歴 / タスク を商談画面から登録

### 4.4 活動履歴（`/activities`）

#### 機能
顧客・担当者・商談に対する営業アクション（電話・訪問・メールなど）を記録。

#### 主な操作
- 「+ 活動を記録」 → 種別・日時・メモを入力
- フィルタ: 顧客別 / 担当者別 / 期間別
- ダッシュボードで直近活動を確認

### 4.5 タスク管理（`/tasks`）

#### 機能
営業タスクの登録・期限管理。期限超過タスクはサイドバーに赤バッジで通知。

#### 主な操作
- 新規タスク登録（期限・優先度・担当者）
- ステータス更新（未着手 / 進行中 / 完了）
- 期限超過のフィルタ表示: `?due_filter=overdue`

> 期限切れタスクが発生すると、ダッシュボードとサイドバー両方にトースト通知が表示されます。

![CRMメニュー](images/crm-menu.png)

---

## 5. 請求書管理【SES限定】

> 本グループは SES 契約機能を有効化したテナントのみ表示されます。

### 5.1 SES台帳（`/ses-contracts`）

#### 機能
SES 契約（技術者派遣）の管理台帳。技術者・顧客・契約期間・単価を一元管理。

#### 主な操作
- 新規 SES 契約の登録（技術者・顧客・開始日・単価・精算条件）
- 契約一覧の検索（顧客名・技術者名・タイトル：ilike 部分一致）
- 契約期間更新・終了処理

### 5.2 勤務表（`/timesheets`）

#### 機能
SES 契約ごとの月次稼働時間を入力・管理。

#### 主な操作
- 月選択 → 各 SES 契約の稼働時間を入力（YYYY-MM 単位）
- 一括更新可能
- 削除ボタンで該当月のレコードを削除

### 5.3 請求書一覧（`/invoices`）

#### 機能
発行済み請求書の一覧表示・PDF 取得。

#### 主な操作
- 月別・顧客別 フィルタ
- 請求書 PDF をダウンロード
- ステータス（未送付 / 送付済 / 入金済）の更新

### 5.4 請求集計（`/billing-summaries`）

#### 機能
月次請求の集計表示。SES 契約と勤務表から自動計算。

#### 主な操作
- 集計対象月を選択
- 顧客ごとの請求金額を確認
- 「請求書を発行」で `/invoices` への自動連携

![請求書管理](images/invoicing.png)

---

## 6. マーケット【SES限定】

### 6.1 案件（`/public-projects`）

#### 機能
公開されている案件情報の管理。手動登録 / 案件メール由来。

#### 主な操作
- **一覧**: 検索（タイトル・説明：ilike 部分一致）/ ステータス絞り込み
- **新規登録**: タイトル・説明・単価・勤務地・必要スキル・案件主
- **編集**: 詳細画面から
- **マッチング**: 案件詳細から「マッチング候補を表示」

#### スキル指定
ProjectRequiredSkill を介して必要スキルを複数登録可能。

### 6.2 技術者（`/engineers`）

#### 機能
登録技術者の管理。プロフィール・スキル・希望条件・名刺画像。

#### 主な操作
- **一覧**: 検索（名前・所属：ilike 部分一致）/ スキル絞り込み
- **新規登録**: 3 タブ構成
  - 基本情報（氏名のみ必須）
  - スキル（経験年数・習熟度）
  - 希望条件（単価・稼働日・勤務形態）
- **マッチング**: 技術者詳細から「マッチング候補案件を表示」

#### マッチング判定ロジック
詳細は [`docs/420_matching_requirements.md`](420_matching_requirements.md) を参照。  
重要な前提:
- 希望単価未記載・35万/月未満の技術者は本番マッチング対象から除外
- マッチ案件表示条件: `案件.unit_price_max >= 技術者希望.unit_price_max`

![マーケット](images/market.png)

---

## 7. メール

### 7.1 メール（`/emails`）

#### 機能
受信メールの一覧表示。Gmail OAuth 連携で 15 分間隔の自動取込。

#### 表示要素
- 未読バッジ（サイドバーのメールアイコンにも表示）
- 件名・差出人・受信日時
- カテゴリラベル: `engineer`（技術者メール）/ `project`（案件メール）/ `other`

#### 主な操作
- 検索（件名・差出人・本文：ilike 部分一致）
- カテゴリで絞り込み
- 既読/未読切り替え
- 添付ファイルダウンロード（メール詳細から）
- 個別削除（関連レコードも一括削除）

#### Gmail 連携
初回利用時は `/api/v1/gmail/callback` で OAuth 認証を実施。  
詳細フローは [`docs/410_feature_design.md`](410_feature_design.md) 参照。

### 7.2 案件メール（`/project-mails`）【SES限定】

#### 機能
分類済みの案件メールを案件メール画面でスコアリング表示。

#### スコア判定（詳細: `docs/540_project_mail_flow.md`）
| スコア | ステータス |
|---|---|
| 60点以上 | 新着案件 |
| 40〜59点 | 要確認 |
| 39点以下 | 除外 |

#### 主な操作
- ステータスで絞り込み
- 詳細画面で「案件確定」「除外」操作
- 「提案メール」ボタンから配信フローへ
- 全件再スコア（手動）

### 7.3 技術者メール（`/engineer-mails`）【SES限定】

#### 機能
分類済みの技術者メールをスコアリング表示。スキル抽出・添付（スキルシート）の解析。

#### スコア判定（詳細: `docs/530_engineer_mail_flow.md`）
| スコア | ステータス |
|---|---|
| 60点以上 | 新着 |
| 40〜59点 | 要確認 |
| 39点以下 | 除外 |

#### 主な操作
- ステータス変更ボタン
- **「技術者として登録→」ボタン**: 技術者管理に自動プリフィル登録
- 全件再スコア（手動・添付解析スキップ）

> 分類とスコアリングは別ロジックです（`docs/530_engineer_mail_flow.md` §8 補足参照）。

![メール画面](images/emails.png)

---

## 8. 配信管理（`/deliveries`）【SES限定】

### 8.1 機能

一斉配信メール（営業案内・技術者紹介・案件提案）の作成・送信・履歴管理。  
AWS SES（東京リージョン）経由で送信。

### 8.2 配信先リスト

#### 主な操作
- CSV インポートで一括登録
- 配信停止リクエスト（unsubscribe）の自動反映
- 個別の `is_active` フラグでオプトアウト

### 8.3 キャンペーン作成

#### 主な操作
1. 件名・本文を入力（プレースホルダ `<%Name%>` 使用可）
2. 添付ファイルを追加（任意）
3. 「送信」をクリック
4. バックグラウンドで送信開始

> 送信中はサイドバーに進捗バー、ダッシュボードに件数表示。

### 8.4 送信履歴

`delivery_send_histories` に保存。  
送信成功・失敗・返信紐づけを `send_type` 別に確認可能。

### 8.5 提案スレッド

`/api/v1/proposal-threads` 経由で、案件メール・技術者メールに紐づく送信履歴をスレッド化表示。  
返信メールの未読有無も視覚化。

### 8.6 配信枠

| 項目 | 値 |
|---|---|
| AWS SES 本番承認 | 2026-04-17 |
| 当面の運用目標 | 日次 12,000 通 / 月次 240,000 通 |
| 送信レート | 14 通/秒（実装は 100ms 間隔の安全マージン） |

詳細: [`docs/450_distribution_management.md`](450_distribution_management.md)

![配信管理](images/deliveries.png)

---

## 9. 名刺管理（`/business-cards`）

### 9.1 機能

紙の名刺を画像アップロード → Google Vision API（OCR）→ 担当者・顧客・メールアドレスを自動抽出。

### 9.2 主な操作

- 単一画像のアップロード（自動回転対応）
- **複数名刺の一括取込**（1 枚に複数名刺が並ぶ画像も Vision API で自動分割）
- 重複検出・統合
- 抽出結果を「担当者」として登録

### 9.3 注意点

- 対応形式: JPEG / PNG（HEIC は事前変換）
- 抽出精度: 印刷の鮮明さに依存
- 複数名刺画像は自動グリッド分割（commit `e6d1720`）

![名刺管理](images/business-cards.png)

---

## 10. 設定

### 10.1 メール署名設定（`/settings/email-template`）

#### 機能
配信メール・提案メールの差出人署名を編集。

#### 主な操作
- テキスト形式で署名本文を編集
- プレースホルダ `<送信者氏名>` `<送信者部署>` などを利用
- 保存後、新規キャンペーンに自動適用

> 未置換プレースホルダがあるとキャンペーン送信時に 422 エラー（`/api/v1/delivery-campaigns` で検証）。

### 10.2 請求書発行元設定（`/settings/invoice-issuer`）【SES限定】

#### 機能
発行する請求書の発行元情報（自社情報）を設定。

#### 入力項目
- 会社名・住所・電話番号
- 適格請求書発行事業者番号（インボイス対応）
- 銀行口座情報
- 印影画像（任意）

![設定](images/settings.png)

---

## 11. 管理【管理者限定】

> サイドバー下部「管理」グループは `super_admin` または `tenant_admin` ロールのみ表示。

### 11.1 ユーザー管理（`/admin/users`）

#### 機能
テナント内ユーザーの追加・編集・無効化。

#### 主な操作
- 新規ユーザー招待（メール送信）
- ロール変更（tenant_admin / tenant_user）
- パスワードリセット送信
- ユーザー無効化

#### ロール別権限
| ロール | 権限 |
|---|---|
| super_admin | 全テナント横断（社内のみ） |
| tenant_admin | 自テナント内ユーザー管理・全機能 |
| tenant_user | 通常業務（管理メニュー非表示） |

### 11.2 データ統計（`/admin/stats`）

#### 機能
テナント単位のデータ概況。

#### 表示要素
- 各テーブルのレコード数（顧客・商談・メール・配信履歴 等）
- メール分類比率
- 送信成功率の月次推移

![管理メニュー](images/admin.png)

---

## 12. よくある質問・トラブルシューティング

### 12.1 ログイン関連

**Q. パスワードリセットメールが届かない**  
A. 迷惑メールフォルダを確認。SES 経由で送信のため `outsource@aizen-sol.co.jp` を許可リスト追加。

**Q. ログイン後すぐにログアウトされる**  
A. ブラウザの Cookie 設定を確認。第三者 Cookie ブロックを解除。

### 12.2 メール関連

**Q. Gmail のメールが取り込まれない**  
A.  
1. 設定で Gmail OAuth が有効か確認
2. 自動同期は 15 分間隔。手動同期は不要（廃止）
3. それでも取り込まれない場合は管理者に連絡

**Q. 技術者メールなのに案件メールに分類された**  
A. キーワード未網羅の可能性あり。管理者に該当メールの件名と本文の特徴を共有してください。分類ロジックは `EmailClassificationService` で随時拡張可能。

### 12.3 配信関連

**Q. 配信先リストの一部にメールが届かない**  
A.  
1. `is_active = false` になっていないか確認（unsubscribe リクエスト時に自動 OFF）
2. SES バウンス履歴を確認（管理者メニュー）
3. `outsource@aizen-sol.co.jp` への返信が滞留している場合は KAGOYA 側を確認

**Q. 配信中に進捗が止まる**  
A. ダッシュボードの「配信中」表示が更新されているか確認。サーバー側のキャッシュキー `campaign_sending_{id}` が残っていれば送信継続中。

### 12.4 マッチング関連

**Q. 期待した技術者がマッチング候補に出ない**  
A.  
- 希望単価未記載・35万/月未満は除外対象
- 案件 `unit_price_max` が技術者希望 `unit_price_max` 以上である必要あり
- 詳細は `docs/420_matching_requirements.md` を参照

### 12.5 操作・表示の不具合

問題発生時は以下の情報を管理者に共有してください：

1. 発生した URL
2. 操作内容
3. ブラウザ・OS のバージョン
4. スクリーンショット（可能であれば）

---

## 13. 関連ドキュメント

| 番号 | タイトル | 用途 |
|---|---|---|
| 050 | PM 計画書 | 全体ロードマップ |
| 220 | Sentry 導入手順 | 障害監視（運用者向け） |
| 230 | バックアップ・リストア手順書 | 障害復旧（運用者向け） |
| 310 | 配信管理デモ資料 | 営業向け説明資料 |
| 410 | メール機能 機能設計書 | 技術仕様（開発者向け） |
| 420 | SES マッチング機能 設計書 | スコアロジック詳細 |
| 450 | 配信管理・AWS SES | 送信枠・本番運用 |
| 510 | メール保持・削除フロー | 営業向け補足 |
| 520 | マッチングフロー Q&A | 営業向け Q&A |
| 530 | 技術者メール フロー | 技術者メール詳細 |
| 540 | 案件メール フロー | 案件メール詳細 |
| 610 | 不達メール一覧 | 運用記録 |

---

*最終更新: 2026-05-03 / 社内リリース 2026-05-07 向け 初版*
